# 07. 스파이크 결과 — pcmap 내장 JSON 필드 실측 (2026-09-14 밤, 이 서버 IP)

- 대상: 키세카츠 안국역점(2086785604)·택산가든(1294253380)·키세카츠 신사역점(1186508365) × 탭 home/review/feed/photo + 안국역점 information. 전부 200, `window.__APOLLO_STATE__` 존재. 픽스처 `test/fixtures/*.html`(13개).
- 클라우드 IP 확인(§7-1)은 VM 구매 후 별도.

## 06번 §7 확인 목록 판정

| # | 항목 | 결과 | 위치 |
|---|---|---|---|
| 2 | 주 엔터티 연결 | ✅ `ROOT_QUERY.placeDetail({...id})` 객체가 base·menus·subwayStations·fsasReviews·informationTab 등을 참조 | — |
| 3 | 대표키워드 | ✅ `placeDetail.informationTab.keywordList: string[]`(5개) | home |
| 4 | 상세설명 | ✅ **`/information` 탭** `placeDetail.description`(안국역점 실제 장문). home의 `base.description`은 항상 '' — 쓰지 말 것 | information |
| 5 | 찾아오는 길 | △ `placeDetail.accessor.accessor`(3매장 모두 '' , hasAccessor:true) — 빈 값이 미등록인지 미확정 → empty=unknown | home |
| 6 | 홈페이지·SNS | ✅ `placeDetail.homepages.repr.url`(+type '인스타그램'), `etc[]`, `relatedLinks[]`(식신·다이닝코드 — 사장님 등록 아님, B7 제외) | home |
| 7 | 영업시간 | ✅ `placeDetail.newBusinessHours[0].businessHours[]` = `{day:'월', businessHours:{start,end}, breakHours:[{start,end}], lastOrderTimes}` 요일별. `base.hideBusinessHours` boolean. `base.openingHours`는 null(구필드) | home |
| 8 | 메뉴판 사진 | ✅ `placeDetail.menuImages[{imageUrl}]` (안국 1, 택산 3+). `Menu[].images[]` 별도 | home |
| 9 | 사진 수 구분 | ✅ `placeDetail.images.totalImages`(안국 23, 택산 16) vs `topPhotos.total`(93, 74 — 방문자·클립 포함). **B5 = images.totalImages**(업체 등록 추정, 항목 id `_business_`) | photo |
| 10 | 소식 | ✅ `ROOT_QUERY.feeds({businessId,...}).feeds[]` → `Feed:{createdString:'20260721', category:'알림'|'EVENT'|'NEW', period:'2026.04.02. ~ 2026.05.31.'|'2026.07.20.'|null, isPinned, title, desc}`. **최신순 정렬**, `hasMore` 플래그 → complete=!hasMore | feed |
| 11 | 방문자 리뷰 날짜·답글 | ❌ `/review` SSR에 VisitorReview 엔터티 없음(클라이언트 XHR). `base.visitorReviewsTotal`·`visitorReviewsScore`만. **D1 날짜·D2 = 보충 질문 고정** | — |
| 12 | 네이버 예약·쿠폰 | ✅ `placeDetail.businessTools.tools[{type:'naverBooking'|'smartCall'|'smartOrder', using:boolean}]` ← **C1·C2의 정답 원천**. `naverBooking.naverBookingUrl`(택산 null). `hasCoupon.count`(안국 1, 택산 0) — 기간 없음 | home |
| 13 | 편의시설·주차 | ✅ `base.conveniences[]`, `informationTab.parkingInfo.description`(문장 — Q&A 주차 문단 원천), `facilities[]` | home |
| 14 | 톡톡·스마트콜 활성 | ✅ `businessTools.smartCall.using`, `base.talktalkUrl` | home |
| 15 | type 불일치 경로 | 미확인(다음 스파이크 — 카페 매장 URL 필요) | — |
| 16 | Menu.recommend·price | ✅ `Menu:{name, price:'19900'(문자열 숫자), recommend, images[], priority:null}` 배열 순서 = 화면 순서 | home |
| 17 | 역 순서 | `subwayStations` 1개만 관측(안국). 복수 시 순서 의미 미확인 → 첫 항목 사용, "인근" 표현만 | home |
| 18 | category 값 | `base.category:'돈가스'` 단일 문자열, `categoryCodeList` 별도 | home |
| 19 | 소식 완전성 | ✅ hasMore | feed |
| 20 | 쿠폰 기간 | ❌ count만. EVENT 소식 period로 대체 | — |

## 결정 (계약 08번 반영)

- 탭 예산 4: **home → information → feed → photo**. review 탭 제거.
- C1 = `businessTools.naverBooking.using`, C2 = `smartCall.using || talktalkUrl`.
- D1 = 총수 0이면 fail, 아니면 unknown(보충). D2 = 보충.
- `missingInfo`(isBizHourMissing 등)는 실측과 모순(영업시간 있는데 true) → 사용 금지.

## 추가 실측 (2026-09-14 밤, 태양 요청 "리뷰도 가져와서 장점 5·보완 5")

- `/review/visitor` 탭 SSR에 **방문자 리뷰 있음** (`/review`에는 없었음): `ROOT_QUERY.visitorReviews(...)` 2개(본문 포함 10 + 미포함 10) → `VisitorReview` 20건 = `{visited:'9.12.토', created, rating, body, reply:{body, created}, votedKeywords[], visitCategories[]}`. 날짜는 **연도 없음** → 기준일로 추정(미래면 작년). 페이지네이션은 `cursor`(GraphQL 별도 호출 필요) → v1은 최근 20건만.
- `visitorReviewStats(...)`: `review.totalCount/avgRating`, `analysis.themes[{label,count}]`(맛·만족도·서비스·분위기…), `analysis.menus`, `analysis.votedKeyword.details[{displayName,count}]`(전체 리뷰 투표 키워드 — "음식이 맛있어요 1312") ← **장점 5의 결정론 원천**.
- 결과: D1(최신 방문일)·D2(최신 답글일) **자동 판정으로 승격**(보충 질문에서 제외). 탭 예산 4→**5**(홈+리뷰+정보+소식+사진), 처리량 상한 480매장/시. 단축 URL은 사진 탭 생략.
- 리뷰 인사이트(lib/review-insight.js): 결정론 = 투표 키워드 상위 5(장점) + 답글 누락·별점≤3 발췌·언급 적은 테마·아쉬움 어휘(보완). 선택 = `ANTHROPIC_API_KEY` 있으면 최근 본문 10건을 LLM(`REVIEW_MODEL`, 기본 claude-opus-5)이 읽고 문장 5/5 생성(JSON 스키마 강제, 실패 시 결정론만).
